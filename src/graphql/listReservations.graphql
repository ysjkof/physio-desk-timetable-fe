query listReservations($input: ListReservationsInput!) {
  listReservations(input: $input) {
    ok
    totalCount
    results {
      id
      startDate
      endDate
      state
      memo
      therapist {
        id
        name
      }
      patient {
        id
        name
        gender
        registrationNumber
        birthday
      }
      lastModifier {
        id
        email
        name
      }
      group {
        id
        name
      }
      prescriptions {
        name
        requiredTime
        description
        price
        activate
      }
    }
  }
}
