query getReservation($input: GetReservationInput!) {
  getReservation(input: $input) {
    error
    ok
    reservation {
      id
      startDate
      endDate
      state
      memo
      user {
        id
        name
        email
      }
      patient {
        id
        name
        gender
        registrationNumber
        birthday
      }
      clinic {
        id
        name
      }
      lastModifier {
        id
        name
        email
      }
    }
  }
}
