query getStatistics($input: GetStatisticsInput!) {
  getStatistics(input: $input) {
    error
    ok
    dayCounts {
      date
      prescriptions {
        name
        count
      }
    }
    totalBundleList {
      name
      count
    }
    totalOptionList {
      name
      count
    }
  }
}
