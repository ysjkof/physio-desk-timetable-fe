query getStatistics($input: GetStatisticsInput!) {
  getStatistics(input: $input) {
    error
    ok
    results {
      userName
      statistics {
        date
        prescriptions {
          name
          count
        }
      }
    }
    prescriptionInfo {
      id
      name
      requiredTime
      price
    }
  }
}
