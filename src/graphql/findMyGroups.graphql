query findMyGroups {
  findMyGroups {
    ok
    error
    groups {
      id
      name
      members {
        id
        staying
        manager
        accepted
        user {
          id
          name
        }
        group {
          id
          name
        }
      }
    }
  }
}
