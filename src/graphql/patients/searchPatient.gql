query searchPatient($input: SearchPatientInput!) {
  searchPatient(input: $input) {
    error
    ok
    totalPages
    totalCount
    patients {
      id
      name
      gender
      registrationNumber
      birthday
      clinic {
        id
        name
      }
      users {
        id
        name
      }
    }
  }
}
