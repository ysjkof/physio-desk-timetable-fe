query getPatient($input: GetPatientInput!) {
  getPatient(input: $input) {
    ok
    error
    patient {
      id
      name
      gender
      registrationNumber
      birthday
      memo
    }
  }
}
