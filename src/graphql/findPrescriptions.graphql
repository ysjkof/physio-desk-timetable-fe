query findPrescriptions($input: FindPrescriptionsInput!) {
  findPrescriptions(input: $input) {
    ok
    error
    optionResults {
      id
      name
      requiredTime
      description
      price
      prescription {
        name
      }
      activate
    }
    bundleResults {
      id
      name
      requiredTime
      description
      price
      prescriptionOptions {
        id
        name
        requiredTime
        description
        price
        prescription {
          name
        }
        activate
      }
      activate
    }
  }
}
