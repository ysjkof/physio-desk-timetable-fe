query findClinicById($input: FindClinicByIdInput!) {
  findClinicById(input: $input) {
    ok
    error
    clinic {
      id
      name
      members {
        id
        staying
        manager
        accepted
        user {
          id
          name
          email
        }
      }
    }
  }
}
